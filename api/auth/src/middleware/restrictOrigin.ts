/*
const cors = require("cors");
const { PX_DOMIN } = require("../configs/env.config");
const allowedOrigins = [
  PX_DOMIN,
  "*.crtil.com",
  "https://crtil.com",
  "https://px.crtil.com",
  "https://cdn.crtil.com",
  "github.com",
  "*.github.com",
];

const corsOptions = {
  //To allow requests from client
  origin: allowedOrigins,
  credentials: true,
};

module.exports = cors(corsOptions);
*/
